name: Blog source guard

on:
  pull_request:
  push:

jobs:
  guard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Reject portfolio blog edits
        shell: bash
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            files="$(git diff --name-only --diff-filter=AM "${{ github.event.pull_request.base.sha }}" "${{ github.event.pull_request.head.sha }}" -- 'content/blog/**' 'content/.remote-blog/**')"
          else
            files="$(git diff --name-only --diff-filter=AM "${{ github.event.before }}" "${{ github.sha }}" -- 'content/blog/**' 'content/.remote-blog/**')"
          fi
          if [ -n "$files" ]; then
            echo "Blog content is owned by the abdspace-blog repository. Publish writings through https://blogs.abdspace.xyz/admin."
            echo "$files"
            exit 1
          fi
